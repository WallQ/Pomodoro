import { type NextPage } from 'next';
import Head from 'next/head';
import { Fragment } from 'react';

const SignIn: NextPage = () => {
	return (
		<Fragment>
			<Head>
				<title>Pomodoro - Sign In</title>
			</Head>
            <h1>Sing In!</h1>
		</Fragment>
	);
};

export default SignIn;
